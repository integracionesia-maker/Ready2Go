import { EmptyState } from "@/design";

// Placeholder de I4a (esqueleto de rutas) — reemplazado por la vista real en I4g.
export default function FichaPrestamoPage() {
  return <EmptyState title="Ficha de préstamo" message="Este módulo se construye en I4g." />;
}
