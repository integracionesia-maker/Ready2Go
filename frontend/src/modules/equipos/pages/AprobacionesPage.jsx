import { EmptyState } from "@/design";

// Placeholder de I4a (esqueleto de rutas) — reemplazado por la vista real en I4e.
export default function AprobacionesPage() {
  return <EmptyState title="Aprobaciones" message="Este módulo se construye en I4e." />;
}
