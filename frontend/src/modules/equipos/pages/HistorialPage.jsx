import { EmptyState } from "@/design";

// Placeholder de I4a (esqueleto de rutas) — reemplazado por la vista real en I4f.
export default function HistorialPage() {
  return <EmptyState title="Historial" message="Este módulo se construye en I4f." />;
}
