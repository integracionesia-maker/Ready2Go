import { EmptyState } from "@/design";

// Placeholder de I4a (esqueleto de rutas) — reemplazado por la vista real en I4d.
export default function ActivosPage() {
  return <EmptyState title="Préstamos activos" message="Este módulo se construye en I4d." />;
}
