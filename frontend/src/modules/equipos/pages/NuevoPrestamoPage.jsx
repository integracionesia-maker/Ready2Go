import { EmptyState } from "@/design";

// Placeholder de I4a (esqueleto de rutas) — reemplazado por el wizard real en I4c.
export default function NuevoPrestamoPage() {
  return <EmptyState title="Nuevo préstamo" message="Este módulo se construye en I4c." />;
}
