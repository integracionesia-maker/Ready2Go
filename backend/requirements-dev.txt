# Dependencias solo para pruebas (no se instalan en producción).
-r requirements.txt
pytest>=8.3.0
httpx>=0.27.0
# Congelar el reloj en las pruebas de atraso/vencimiento (America/Mexico_City).
freezegun>=1.5.0
# Leer el PDF generado en las pruebas: sin esto solo se puede comprobar que el
# archivo existe, no que diga lo que la carta responsiva tiene que decir.
pypdf>=5.0.0
