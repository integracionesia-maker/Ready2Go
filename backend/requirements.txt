fastapi>=0.115.0
uvicorn[standard]>=0.34.0
sqlalchemy>=2.0.36
pydantic>=2.11.0
python-multipart>=0.0.19
pyjwt>=2.9.0
argon2-cffi>=23.1.0
email-validator>=2.2.0
# Control de Equipos: PDF de la carta responsiva (reportlab) y miniaturas +
# validacion de imagen del modulo media (pillow).
reportlab>=4.2.0
pillow>=11.0.0
# zoneinfo no trae la base IANA en Windows: sin tzdata, ZoneInfo("America/Mexico_City")
# revienta y el calculo de atraso se queda sin zona. En Linux/macOS suele estar en
# el sistema, pero se declara para que un entorno limpio no dependa de la suerte.
tzdata>=2024.1
